//We can extend the size of array by using vector function but it will increase the time complexity.

//Contigous memory allocation we can do but if we have short of memory we cannot do.If
//we have enough memory it can take directly from the middle or between

//It willmake fixed size an unlimited.


#include<iostream>
using namespace std;
